{
  "name": "netlifytrading",
  "version": "1.0.0",
  "scripts": {
    "build": "netlify build",
    "dev": "netlify dev",
    "start": "netlify dev",
    "deploy": "netlify deploy --build",
    "deploy:prod": "netlify deploy --build --prod",
    "functions": "netlify functions:serve"
  },
  "dependencies": {
    "netlify-cli": "^12.0.0"
  }
}
